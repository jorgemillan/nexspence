{{- if eq .Values.exposure.type "ciliumGateway" -}}
---
# K8s Gateway API Gateway (implemented by Cilium GatewayClass)
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: {{ include "nexspence.fullname" . }}-cilium
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "nexspence.labels" . | nindent 4 }}
  {{- with .Values.exposure.ciliumGateway.gatewayAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  gatewayClassName: {{ .Values.exposure.ciliumGateway.gatewayClassName }}
  listeners:
    {{- if .Values.exposure.ciliumGateway.tls }}
    - name: https
      protocol: HTTPS
      port: 443
      tls:
        mode: Terminate
        {{- with (index .Values.exposure.ciliumGateway.tls 0).certificateRefs }}
        certificateRefs:
          {{- toYaml . | nindent 10 }}
        {{- end }}
      allowedRoutes:
        namespaces:
          from: Same
    {{- else }}
    - name: http
      protocol: HTTP
      port: 80
      allowedRoutes:
        namespaces:
          from: Same
    {{- end }}
---
# K8s Gateway API HTTPRoute
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "nexspence.fullname" . }}-cilium
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "nexspence.labels" . | nindent 4 }}
spec:
  parentRefs:
    - name: {{ include "nexspence.fullname" . }}-cilium
      namespace: {{ .Release.Namespace }}
  hostnames:
    {{- range .Values.exposure.hosts }}
    - {{ .host | quote }}
    {{- end }}
  rules:
    - matches:
        {{- range .Values.exposure.hosts }}
        {{- range .paths }}
        - path:
            type: {{ .pathType }}
            value: {{ .path }}
        {{- end }}
        {{- end }}
      backendRefs:
        - name: {{ include "nexspence.fullname" . }}
          port: {{ .Values.service.port }}
          weight: 100
{{- end }}
